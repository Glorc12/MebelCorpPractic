# MebelCorp — Система управления мебельным производством

Проект разработан в рамках учебной практики. Система автоматизирует учет продукции, управление цехами и расчет необходимых материалов с учетом коэффициентов брака.

## 🛠 Стек технологий
*   **Backend:** Python 3.10+, Flask, SQLAlchemy
*   **Database:** PostgreSQL
*   **Frontend:** HTML5, CSS3, Jinja2, Bootstrap (или кастомные стили)
*   **Tools:** Git, Pandas (для импорта данных)

## 📋 Реализованный функционал
1.  **Продукция:** Просмотр списка, фильтрация, сортировка.
2.  **Управление:** Добавление новых товаров и редактирование существующих.
3.  **Логика:**
    *   Подсчет времени производства (сумма времени по всем цехам).
    *   Расчет количества сырья (с учетом типа продукции и % брака).
4.  **Цехи:** Просмотр загруженности производственных площадок.

## 🚀 Запуск проекта

1.  **Клонировать репозиторий:**
    ```
    git clone https://github.com/Glorc12/MebelCorpPractic.git
    cd MebelCorpPractic
    ```

2.  **Настроить виртуальное окружение:**
    ```
    python -m venv venv
    source venv/bin/activate  # Для Windows: venv\Scripts\activate
    pip install -r requirements.txt
    ```

3.  **База данных:**
    *   Создайте БД `mebel_corp` в PostgreSQL.
    *   В файле `config.py` укажите свои данные для подключения.
    *   Выполните миграции и импорт данных:
        ```
        python create_db.py
        python import_data.py
        ```

4.  **Запуск сервера:**
    ```
    python app.py
    ```
    Приложение доступно по адресу: `http://127.0.0.1:5000`

---
© 2006–2025 MebelCorp
